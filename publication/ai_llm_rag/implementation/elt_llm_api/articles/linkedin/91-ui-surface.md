A chat box without sources is a demo.
If the UI hides evidence, trust collapses on the first challenge.
So citations are a first-class UI element.

Most internal RAG projects fail for a boring reason:

The only usable interface is a command line.

The second failure is worse: the UI exists, but it hides evidence. Users see an answer, not the sources. Trust collapses the first time the answer is challenged.

The turning point is treating sources as a first-class UI element:
- show the active profile (what data is being searched)
- show the answer
- show citations/sources alongside it

📌 That’s the difference between “it answered” and “we can trust it”.

Proof points to capture before publishing:
- [NEEDS_DATA: publish-safe screenshots from a public dataset run]

Question: in your internal assistants, can users see the evidence without leaving the UI?

#Product #SoftwareEngineering #AI #UX #DataEngineering
